package main.com.pieisspy.tablefortress.view.components;

import javax.swing.*;
import java.awt.*;
/*
public class Prompt extends JPanel {
    public Prompt() {
        currentPanel
    }

    private CardLayout cardLayout = new CardLayout();
    private JPanel currentPanel = new JPanel(cardLayout);
    private JPanel choicePanel = new JPanel(new GridLayout(2,1));
    private JPanel cancelPanel = new JPanel(new BorderLayout());
}

 */
